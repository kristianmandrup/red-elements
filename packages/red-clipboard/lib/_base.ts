export { Context, $ } from '../../context';
export const { log } = console
export { Clipboard } from './'
export {
  container,
  delegateTarget,
  delegator,
  delegateTo,
  lazyInject,
  $TYPES,
} from './container'
