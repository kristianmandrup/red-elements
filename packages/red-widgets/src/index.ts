import {
  canvas,
  Canvas
} from './canvas'

import {
  Validators
} from './validators'

import {
  common,
  Tabs,
  Stack,
  Menu,
  Popover,
  Searchbox,
  EditableList,
  CheckboxSet,
  TypedInput
} from './common'

import {
  header
} from './header'

import {
  library,
  Library,
  LibraryUI
} from './library'

import {
  Main,
} from './main'

import {
  User,
} from './user'

import {
  TextFormat,
  Bidi
} from './text'

import {
  Actions
} from './actions'

import {
  Clipboard
} from './clipboard'

import {
  Deploy
} from './deploy'

import {
  Keyboard
} from './keyboard'

import {
  Notifications
} from './notifications'

import {
  STATE
} from './state'

import {
  Utils
} from './utils'

import {
  diff,
  NodeDiff
} from './node-diff'

import {
  editor,
  NodeEditor
}
  from './node-editor'

import {
  Workspaces,
  workspaces
} from './workspaces'

import {
  palette,
  Palette,
  PaletteEditor
} from './palette'

import {
  sidebar,
  Sidebar,
  SidebarTabConfig,
  SidebarTabInfo,
  Tips,
} from './sidebar'

import {
  settings,
  UserSettings
} from './settings'

import {
  RadialMenu
} from './touch'

import {
  tray,
  Tray
} from './tray'

import {
  search,
  Search,
  TypeSearch
} from './search'

export const libs = {
  canvas,
  common,
  library,
  editor,
  diff,
  palette,
  search,
  settings,
  sidebar,
  workspaces
}

export const widgets = {
  common,
  Tabs,
  Stack,
  Menu,
  Popover,
  Searchbox,
  EditableList,
  CheckboxSet,
  TypedInput,
  Main,
  User,
  Utils,
  Actions,
  Deploy,
  Keyboard,
  TextFormat,
  Bidi,
  Canvas,
  Notifications,
  Clipboard,
  RadialMenu,
  NodeEditor,
  Palette,
  PaletteEditor,
  NodeDiff,
  Library,
  LibraryUI,
  Tray,
  Sidebar,
  SidebarTabConfig,
  SidebarTabInfo,
  Tips,
  Search,
  TypeSearch,
  UserSettings,
  Workspaces,
  Validators
}

export default libs

export {
  Main,
  User,
  Menu,
  Stack,
  Popover,
  Searchbox,
  EditableList,
  CheckboxSet,
  TypedInput,
  Utils,
  Canvas,
  Actions,
  Deploy,
  Keyboard,
  TextFormat,
  Bidi,
  Notifications,
  Clipboard,
  RadialMenu,
  NodeEditor,
  Palette,
  PaletteEditor,
  NodeDiff,
  Library,
  LibraryUI,
  Tray,
  Sidebar,
  SidebarTabConfig,
  SidebarTabInfo,
  Tips,
  Search,
  TypeSearch,
  UserSettings,
  Workspaces,
  Validators
}

