import {
  canvas
} from './canvas'

import {
  common,
  Tabs,
  Stack,
  Menu
} from './common'

import {
  header
} from './header'

import {
  library,
  Library,
  LibraryUI
} from './library'

import {
  Main,
} from './main'

import {
  User,
} from './user'

import {
  TextFormat,
  Bidi
} from './text'

import {
  Actions
} from './actions'

import {
  Clipboard
} from './clipboard'

import {
  Deploy
} from './deploy'

import {
  Keyboard
} from './keyboard'

import {
  Notifications
} from './notifications'

import {
  STATE
} from './state'

import {
  Utils
} from './utils'

import {
  diff,
  NodeDiff
} from './node-diff'

import {
  editor,
  NodeEditor
}
  from './node-editor'

import {
  Workspaces,
  workspaces
} from './workspaces'

import {
  palette,
  Palette,
  PaletteEditor
} from './palette'

import {
  sidebar,
  Sidebar,
  SidebarTabConfig,
  SidebarTabInfo,
  Tips,
} from './sidebar'

import {
  settings,
  UserSettings
} from './settings'

import {
  RadialMenu
} from './touch'

import {
  tray,
  Tray
} from './tray'

import {
  search,
  Search,
  TypeSearch
} from './search'

export const libs = {
  canvas,
  common,
  library,
  editor,
  diff,
  palette,
  search,
  settings,
  sidebar,
  workspaces
}

export const widgets = {
  Main,
  User,
  Utils,
  Menu,
  Actions,
  Deploy,
  Keyboard,
  TextFormat,
  Bidi,
  Notifications,
  RadialMenu,
  NodeEditor,
  Palette,
  PaletteEditor,
  NodeDiff,
  Library,
  LibraryUI,
  Tray,
  Sidebar,
  SidebarTabConfig,
  SidebarTabInfo,
  Tips,
  Search,
  TypeSearch,
  UserSettings,
  Workspaces
}

export default libs

export {
  Main,
  User,
  Menu,
  Utils,
  Actions,
  Deploy,
  Keyboard,
  TextFormat,
  Bidi,
  Notifications,
  RadialMenu,
  NodeEditor,
  Palette,
  PaletteEditor,
  NodeDiff,
  Library,
  LibraryUI,
  Tray,
  Sidebar,
  SidebarTabConfig,
  SidebarTabInfo,
  Tips,
  Search,
  TypeSearch,
  UserSettings,
  Workspaces
}

// export {
//   // canvas,
//   common,
//   // header,
//   // library,
//   // mainContainer,
//   // nodeDiff,
//   // nodeEditor,
//   palette,
//   // search,
//   // settings,
//   // sidebar,
//   tray,
//   // workspaces
// }
