import {
  canvas
}
  from './canvas'

import {
  common
}
  from './common'

import {
  header
}
  from './header'

import {
  library
}
  from './library'

// import {
//   mainContainer
// }
// from './main-container'

import {
  nodeDiff
}
  from './node-diff'

import {
  nodeEditor
}
  from './node-editor'

import {
  palette,
  Palette,
  PaletteEditor
} from './palette'

import {
  tray,
  Tray
}
  from './tray'


export const widgets = {
  // canvas,
  common,
  // library,
  // nodeEditor,
  // nodeDiff
  // palette,
  // search,
  // settings,
  // sidebar,
  tray,
  // workspaces
}

export default widgets

export {
  Palette,
  PaletteEditor,
  Tray
}

export {
  // canvas,
  common,
  // header,
  // library,
  // mainContainer,
  // nodeDiff,
  // nodeEditor,
  palette,
  // search,
  // settings,
  // sidebar,
  tray,
  // workspaces
}
