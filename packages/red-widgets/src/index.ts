import {
  canvas
}
  from './canvas'

import {
  common
}
  from './common'

import {
  header
}
  from './header'

import {
  library
}
  from './library'

// import {
//   mainContainer
// }
// from './main-container'

import {
  nodeDiff
}
  from './node-diff'

import {
  nodeEditor
}
  from './node-editor'

import {
  palette
} from './palette'

export const widgets = {
  // canvas,
  common,
  // library,
  // nodeEditor,
  // nodeDiff
  // palette,
  // search,
  // settings,
  // sidebar,
  // tray,
  // workspaces
}

export default widgets

const { Palette } = palette.controllers

export {
  Palette
}

export {
  // canvas,
  common,
  // header,
  // library,
  // mainContainer,
  // nodeDiff,
  // nodeEditor,
  // palette,
  // search,
  // settings,
  // sidebar,
  // tray,
  // workspaces
}
